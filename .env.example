# Public — safe to ship to the browser bundle (RLS-gated anon key)
NEXT_PUBLIC_SUPABASE_URL=https://gbwusopifbyhlcppbfnl.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server only — NEVER prefix with NEXT_PUBLIC_. Bypasses RLS. Used only in
# app/api/register/route.js and app/api/keepalive/route.js.
SUPABASE_SERVICE_ROLE_KEY=

# Email — league's own Gmail account, sends the signed waiver PDF to the director.
# GMAIL_APP_PASSWORD is a Google "App Password" (requires 2-Step Verification
# on the Gmail account), not the regular Gmail password. See README.md.
GMAIL_USER=afasoftballutah@gmail.com
GMAIL_APP_PASSWORD=
DIRECTOR_EMAIL=
